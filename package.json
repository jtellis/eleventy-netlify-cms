{
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0"
  },
  "scripts": {
    "generate": "eleventy --input=src",
    "serve": "eleventy --input=src --serve --port=1111",
    "watch": "eleventy --watch"
  }
}